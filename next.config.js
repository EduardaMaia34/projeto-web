/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Isso ajuda o Next a entender arquivos antigos
    pageExtensions: ['js', 'jsx', 'ts', 'tsx'],
}

module.exports = nextConfig